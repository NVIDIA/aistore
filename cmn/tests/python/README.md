```console
$ pip3 install -r requirements.txt

# step 1:
$ mkdir /tmp/packed /tmp/unpacked

# step 2: in the msgpack_test.go: uncomment ioutil.WriteFile line and run it

# step 3: execute and see output in the /tmp/unpacked
$ python3 unpack.py
```
