apiVersion: v1
kind: Pod
metadata:
  name: tar2tf-test
  annotations:
    communication_type: "hrev://"
    wait_timeout: 2m
spec:
  hostNetwork: true
  containers:
    - name: server
      image: aistore/tar2tf:latest
      ports:
        - containerPort: 80