name: Lint
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, labeled, ready_for_review]
    branches:
      - main
  workflow_dispatch:

jobs:
  lint:
    strategy:
      matrix:
        go-version: ['1.25.x']
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: ${{ matrix.go-version }}

      - name: Install `golangci-lint`
        uses: golangci/golangci-lint-action@v8
        with:
          # NOTE: See `lint-update-ci` target in Makefile.
          version: v2.5.0
          args: --timeout=30m

      - name: Lint go
        run: |
          export GOPATH="$(go env GOPATH)"
          make lint
          TAGS="nethttp debug oteltracing oci aws gcp azure" make lint
          make fmt-check
          make spell-check
